# Pharmacy-Management-System-AWT-Java
A Java AWT-based Pharmacy Management System for managing medicines, customers, billing, and stock details. The system uses AWT for the GUI and MySQL (via JDBC) for backend data storage. Features include adding, updating, searching medicines, generating bills, and maintaining pharmacy inventory records.
